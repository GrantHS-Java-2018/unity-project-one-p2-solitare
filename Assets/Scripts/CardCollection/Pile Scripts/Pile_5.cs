using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pile_5 : PileScript
{
    // Start is called before the first frame update
    void Start()
    {
        Stack<Card> pileCards = new Stack<Card>(5);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
