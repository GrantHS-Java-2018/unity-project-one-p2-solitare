using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pile_6 : PileScript
{
    // Start is called before the first frame update
    void Start()
    {
        Stack<Card> pileCards = new Stack<Card>(6);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
